<%= form_with(model: calendar) do |form| %>
  <% if calendar.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(calendar.errors.count, "error") %> prohibited this calendar from being saved:</h2>

      <ul>
        <% calendar.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name, placeholder: 'Discipline, vacances...', class: 'form-control form-control-sm' %>
  </div>

  <div>
    <%= form.label :background_color, style: "display: block" %>
    <%= form.color_field :background_color %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
